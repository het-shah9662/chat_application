# chat_application
A real-time chat application built with **React** (frontend) and **Node.js + Socket.IO + SQL Server** (backend). It supports multiple users, typing indicators, and message persistence via SQL Server.
